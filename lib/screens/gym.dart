import 'package:flutter/material.dart';
import '../widgets/gym_widget.dart';

class Gym extends StatelessWidget {
  const Gym({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const GymWidget();
  }
}